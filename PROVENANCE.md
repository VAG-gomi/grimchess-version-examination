# Provenance

This repository is derived from two immutable Git commits. The exact commit IDs, repository names, APK hashes, and method are recorded in [`inputs/PINNED_INPUTS.json`](inputs/PINNED_INPUTS.json) and [`COMPARISON_MANIFEST.json`](COMPARISON_MANIFEST.json).

No file in either evidence repository was edited. No APK was modified, re-signed, normalised, or overwritten. Comparison outputs are newly computed records and must not be treated as original evidence.
