# GrimChess Timeline

| Event or object | What is established | What is not established |
|---|---|---|
| Historical APK set | Four distinct APK hashes are preserved as `OLD-APK-001` through `OLD-APK-004`. | Their chronological order. |
| Historical source archive | Recoverable source/build material exists in the Old evidence repository. | That it exactly produced every preserved APK. |
| Earlier Current-related hash | SHA-256 `61a00ce9…` is byte-identical to `OLD-APK-002`. | Whether its historical label and Old label represent the same release context. |
| Current F1 archive | Pinned at commit `f7cd9a89…`; F1 APK hash is `77f33dc3…`. | Whether all Current historical artifacts are represented by that one APK. |

No chronology is inferred from filenames, filesystem timestamps, repository order, or hash order.
